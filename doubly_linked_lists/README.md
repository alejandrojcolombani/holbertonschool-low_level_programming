This project introduces doubly linked lists in C.

A doubly linked list is a data structure made of nodes, where each node contains:
- Data
- A pointer to the next node
- A pointer to the previous node
