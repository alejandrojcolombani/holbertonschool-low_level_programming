This project introduces linked lists in C.  
A linked list is a data structure made of nodes, where each node stores data and a pointer to the next node.

Unlike arrays, linked lists do not require contiguous memory locations. This makes them flexible when adding or removing elements, especially when the size of the data is not known in advance.

This project focuses on understanding how linked lists work, how to build them, and when to use them instead of arrays.