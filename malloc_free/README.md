This project introduces dynamic memory allocation in C using malloc and free.

Dynamic memory allows programs to request memory while running, instead of defining fixed-size variables at compile time. This is useful when the size of data is unknown in advance.